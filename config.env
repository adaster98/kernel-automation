# ============================
# KERNEL BUILDER CONFIGURATION
# ============================

# --- HARDWARE & DRIVERS ---
# Space-separated list of GPUs.
# Options: "nvidia" "amd" "intel"
# Examples:
#   "amd"           -> In-tree drivers only.
#   "nvidia"        -> Rebuilds & signs external Nvidia modules if needed.
#   "intel nvidia"  -> In-tree Intel + External Nvidia (Dual GPU).
GPU_DRIVERS="amd"

# If true, applies specific CFLAGS/LLVM vars when rebuilding Nvidia drivers
NVIDIA_USE_CUSTOM_CFLAGS="false"

# --- COMPILER SETTINGS ---
# If true, builds kernel and modules with LLVM=1
USE_LLVM="false"

# --- BOOTLOADER ---
# Options: "systemd-boot", "grub", "limine", "none"
BOOTLOADER="systemd-boot"

# --- INITRAMFS ---
# Options: "dracut", "ugrd", "none"
INITRAMFS_TOOL="dracut"

# --- PATHS ---
# Where your EFI partition is mounted (e.g., /efi, /boot, /boot/efi)
ESP_DIR="/efi"

# Location of signing keys (for Secure Boot)
# These are only used if CONFIG_MODULE_SIG is detected in the kernel config.
KEY_PRIV="/usr/src/linux/certs/signing_key.pem"
KEY_PUB="/usr/src/linux/certs/signing_key.x509"

# --- KERNEL COMMAND LINE ---
# Do NOT include "root=..." here. The script detects your root PARTUUID automatically.
# Add your standard flags here.
KERNEL_CMDLINE="rw quiet"
